import { Request, Response } from "express";

const deleteLiestaDeCompras = (resquest: Request, Response: Response) => {};

const deletById = (resquest: Request, Response: Response) => {};

export default { deleteLiestaDeCompras, deletById };
